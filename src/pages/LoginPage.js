import Login from "../components/authentication/Login";
import '../index.css';

const LoginPage = () => {
    return (
        <div>
            <Login />
        </div>
    );
};

export default LoginPage;